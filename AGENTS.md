# AGENTS.md —— 项目协作规则

> 本文件是给 AI 助手的长期行为准则。规则只允许追加，不允许删改已有条目。

## Day 1 基础规则

1. **当天只做当天任务，学员没说「进入下一板块」就不越界。**
2. AI 代办的每个操作都要向学员解说「这一步在做什么、为什么」，把过程当教学。
3. 提交信息（commit）必须以「Day X｜」开头，正文两行：改了什么 / 加了什么。
4. 涉及外部账号（GitHub 登录、授权弹窗）时，必须提醒学员亲自操作确认。
5. 遇到报错先排查原因再动手，把排障过程讲给学员听，不静默重试糊弄过去。

## Day 2 追加规则

6. `.env` 及一切秘密文件（密码、密钥、配置）永远不提交、不上传，由 `.gitignore` 兜底。
7. 今天不做明天的功能：`index.html` 在 MVP（Day 7）前只保持占位页状态。

## Day 6 追加规则

8. **任何文件改动前，必须先执行 `git status`（必要时配合读取文件内容）查看现状，确认要改的位置和影响范围后再动手，禁止不看现场直接修改或覆盖。**
   —— 防的情况：AI 拿到任务就直接写文件，改错位置、覆盖学员或之前版本的内容；也防止误判「文件是空的/丢失」而做出错误的重建决策。

9. **每个页面的改动完成后，必须在手机宽度（约 375px）下检查排版是否正常，不能只在电脑宽屏下验收。**
   —— 防的情况：倒计时、密码生成等功能只在电脑宽屏下测过，手机用户打开排版崩坏，这是网页开发最常见的翻车点。

10. **代码改完不算完成，必须在浏览器里实际打开页面操作验证（含检查控制台无报错），以 PRD 验收标准逐条核对后才允许提交。**
    —— 防的情况：代码「看着对」但实际跑不起来、有运行时报错；对应 PRD 成功指标 3「工具页面无控制台报错」。
