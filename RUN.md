# RUN.md —— 本地运行说明

> Day 7 存档 | 如何在本机把网站跑起来

## 启动命令

在项目文件夹（`D:\workbuddyoutcome\super_study\my-first-website`）打开终端，执行：

```bash
python -m http.server 8000
```

然后在浏览器打开：

```
http://localhost:8000
```

首页出现「效率小工具箱」三个工具入口即启动成功。停止服务：在终端按 `Ctrl + C`。

## 为什么不直接双击 HTML 文件？

纯静态页面双击也能打开，但用本地服务器（localhost）有两个好处：

1. 「复制密码」按钮用的剪贴板接口要求**安全上下文**，localhost 属于安全上下文，行为与线上环境一致
2. 路径行为（相对路径跳转）与将来部署到线上时完全一致，测试结论更可靠

## 页面清单

| 页面 | 路径 | 功能 |
|---|---|---|
| 首页 | `/index.html` | 三个工具的导航入口 |
| 倒计时器 | `/timer.html` | 设置时长 → 开始/暂停/重置 → 归零闪烁+提示音 |
| 单位换算器 | `/converter.html` | 长度/重量/温度互转，输入即出结果 |
| 密码生成器 | `/password.html` | 设置长度和字符集 → 生成 → 一键复制 |

## 技术说明

- 纯前端静态页面，无后端、无数据库、无第三方库
- 所有计算在浏览器本地完成，无任何数据上传（TECH_DESIGN.md）
