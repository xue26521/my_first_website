# PRD —— 效率小工具箱 · MVP 产品需求文档

> Day 4 产出 | 项目：my-first-website | 基于：research.md（Day 3）
> 状态：MVP v0.1 | 技术选型见 Day 5，本文档不涉及实现方式。

## 1. 产品概述

**一句话**：一个打开即用、免登录、无广告的网页版效率小工具箱。

**目标用户**：有「一分钟小需求」的普通电脑用户（包括我自己）——需要倒计时、单位换算、生成密码，但不想为此下载 App 或忍受广告网站。

**核心价值**：从打开网页到拿到结果，不超过 10 秒；不注册、不弹广告、不收集任何数据。

## 2. MVP 范围

本版本**只包含 3 个工具**，每个工具一个独立页面，首页提供工具导航：

1. ⏱️ 倒计时器（Countdown Timer）
2. 📐 单位换算器（Unit Converter）
3. 🔑 密码生成器（Password Generator）

## 3. 功能需求与验收标准

### 3.1 首页（工具导航）

**用户故事**：作为用户，我打开网站就能一眼看到有哪些工具，并点进任意一个。

**验收标准（全部满足才算通过）**：
- [ ] AC1：页面显示 3 个工具入口，每个入口有名称和图标
- [ ] AC2：点击任意入口，能在 1 秒内进入对应工具页面
- [ ] AC3：页面无广告、无弹窗、无登录提示

### 3.2 倒计时器

**用户故事**：作为用户，我想设置一个分钟级倒计时并开始计时，时间到时能明显提醒我。

**验收标准**：
- [ ] AC4：可输入任意的「时:分:秒」，支持键盘输入和快捷预设（1/5/25/60 分钟）
- [ ] AC5：点击开始后，页面每秒刷新剩余时间，格式为 `HH:MM:SS`
- [ ] AC6：提供暂停/继续、重置两个操作
- [ ] AC7：倒计时归零时，页面出现视觉变化（如变色/闪烁）并播放提示音
- [ ] AC8：切到浏览器其他标签页后回来，倒计时时间依然准确（基于目标时间戳而非累加计数）
- [ ] AC9：未开始计时时，时间输入框可正常编辑；计时中修改输入需先重置

### 3.3 单位换算器

**用户故事**：作为用户，我想输入一个数值，选择「从什么单位换到什么单位」，立刻看到结果。

**验收标准**：
- [ ] AC10：首期支持 3 类换算：长度（米/千米/厘米/英寸/英尺/英里）、重量（克/千克/磅/盎司）、温度（摄氏/华氏/开尔文）
- [ ] AC11：输入 3.5 英寸，显示 8.89 厘米，结果保留最多 4 位小数并去除末尾无意义的 0
- [ ] AC12：换算类型切换后，单位下拉列表随之更新，且不残留上次输入的旧结果
- [ ] AC13：输入非法内容（字母、负数不适用于长度/重量时）时，给出明确的错误提示而不是崩溃或显示 NaN
- [ ] AC14：温度换算使用标准公式，0°C = 32°F = 273.15K 验证通过

### 3.4 密码生成器

**用户故事**：作为用户，我想一键生成一个足够强的随机密码，并复制走。

**验收标准**：
- [ ] AC15：可设置密码长度（范围 8–64），默认 16
- [ ] AC16：可勾选字符集：大写字母、小写字母、数字、特殊符号，默认至少勾选其中 3 类
- [ ] AC17：点击生成后立即显示新密码；连续点击 10 次得到 10 个不同密码
- [ ] AC18：密码只包含所选字符集内的字符（可人工抽查验证）
- [ ] AC19：提供「复制」按钮，点击后密码进入剪贴板，按钮有「已复制」反馈
- [ ] AC20：密码生成过程不发送任何网络请求（纯本地随机生成）

## 4. 明确不做（本期范围外）

继承 research.md 的砍需求结论，外加 PRD 阶段新增的细化：

- ❌ 用户注册 / 登录 / 个人中心
- ❌ 后端服务器与数据库
- ❌ 超过 3 个工具（更多工具进「后续版本待定池」：二维码生成、颜色转换、文本 diff 等）
- ❌ 移动 App / 小程序（移动端浏览器可访问即可，但不做专门优化）
- ❌ AI 功能、云同步、历史记录、多语言、工具收藏夹
- ❌ 浏览器关闭后倒计时继续提醒（需要系统通知权限，复杂度高收益低，砍）
- ❌ 密码强度可视化评分（第一版只保证「足够随机」，评分进待定池）

**砍需求理由摘要**：MVP 只需回答「这 3 个工具好用吗」。任何不直接服务这个问题的功能都可能让 Day 7 交付失败。倒计时系统提醒和密码强度评分被砍，是因为它们各自引入新的技术依赖（通知权限、评分算法），收益却不改变 MVP 的核心结论。

## 5. 成功指标（MVP 上线后观察）

1. 自己连续使用 3 个工具各至少 1 次，全程不超过 30 秒且无卡顿、无报错
2. 找 1 位同学使用，不借助任何说明能独立完成三个工具的核心操作
3. GitHub 仓库页面可直接访问，工具页面无控制台报错

## 6. 开放问题（留给 Day 5 技术选型）

1. 三个工具页 + 首页，用什么方式组织页面（多页 or 单页应用）？
2. 提示音的素材来源（内置音频文件 or 程序合成）？
3. 倒计时 AC8 的「基于目标时间戳」方案在不同浏览器下的兼容性如何保证？
4. AC7 提示音在后台标签页可能受浏览器自动播放策略限制——Day 7 测试时需验证「切走再切回时提醒是否触发」，必要时改为视觉提醒兜底（自检发现，Day 5 定方案）。

## 7. 里程碑

| 时间 | 交付 |
|---|---|
| Day 5 | 技术选型确定 |
| Day 6 | 开发准备 |
| Day 7 | MVP 上线，三项成功指标全部达成 |
